upstream api {
    server api:4000;
}

server {
    listen 80;

    location /api/ {
        proxy_pass http://api/graphql;
    }  

    # location /admin/ {
    #     proxy_pass http://api;
    # }      

    location / {
      root /usr/share/nginx/html;
      try_files $uri $uri/ /index.html;
    }

}